import time

def setTime(sec):
    if sec > 60:
        sec = round(sec/60, 0)
        print(sec)
        '''
        while sec:
        i = divmod()
        print('TIME REMAINING: '+ str(sec))
        sec -= 1
        '''

setTime(1240)